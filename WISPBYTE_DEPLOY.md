# دليل رفع وتشغيل تطبيق ماسنجر لايت على استضافة WispByte (wispbyte.com)

تم تهيئة وتوافق التطبيق بنسبة 100% للعمل مباشرة على سيرفرات واستضافة **WispByte** (التي تعتمد على لوحة Pterodactyl / Wings).

---

## 1. متطلبات السيرفر على WispByte
- **نوع السيرفر (Server Type):** Node.js Application / Generic Node.js
- **إصدار Node.js:** Node.js 18 أو 20 أو 22 (الافتراضي `>= 18`)
- **المنافذ (Allocations):** المنفذ الرئيسي المخصص لسيرفرك في لوحة WispByte (يُقرأ تلقائياً عبر `SERVER_PORT` أو `PORT`).

---

## 2. خطوات الرفع على WispByte خطوة بخطوة

### الخطوة الأولى: تجهيز الملفات والرفع
1. قم بتنزيل ملفات المشروع كملف ZIP (عبر خيار التصدير أو من مجلدك).
2. ادخل إلى حسابك في [WispByte Panel](https://wispbyte.com/).
3. افتح سيرفر الـ Node.js الخاص بك وتوجه إلى قائمة **Files** (مدير الملفات).
4. اضغط على **Upload** وارفع ملف الـ ZIP، ثم اضغط على النقاط الثلاث بجانب الملف واختر **Unarchive** (فك الضغط).

### الخطوة الثانية: تثبيت الحزم (Dependencies)
1. افتح صفحة **Console** (الطرفية) في لوحة WispByte.
2. إذا كان السيرفر متوقفاً، قم بتشغيل أمر تثبيت الحزم:
   ```bash
   npm install
   ```
   *(الحزم المستخدمة خفيفة جداً: `express` و `ws` فقط)*

### الخطوة الثالثة: التحقق من إعدادات التشغيل (Startup Settings)
توجه إلى تبويب **Startup** في لوحة WispByte وتأكد من الآتي:
- **Startup Command:**
  ```bash
  node index.js
  ```
  أو:
  ```bash
  npm start
  ```
  *(تم تجهيز كل من `index.js` و `server.js` و `package.json` لتعمل بأي أمر افتراضي تختاره اللوحة دون أي أخطاء)*.

### الخطوة الرابعة: تشغيل السيرفر
1. ارجع إلى صفحة **Console**.
2. اضغط على زر **Start**.
3. ستلاحظ في سجلات التشغيل:
   ```text
   ماسنجر لايت (production)
   Web   : http://0.0.0.0:[PORT]/
   API   : http://0.0.0.0:[PORT]/api/circle
   دائرة : أعضاء (غير محدود)
   ```
4. الآن افتح الرابط المخصص لسيرفرك على WispByte (مثال: `http://nodeXX.wispbyte.com:PORT` أو النطاق المربوط بسيرفرك)، وسيعمل التطبيق والدردشة والويب سوكيت فوراً!

---

## 3. ميزات التوافق المضمنة مع WispByte
- **التعرف التلقائي على المنافذ:** قراءة متغير `process.env.SERVER_PORT` الذي تعينه لوحة WispByte/Pterodactyl بجانب `process.env.PORT`.
- **الاستماع على كل الواجهات:** الارتباط بـ `0.0.0.0` لتمكين الاتصال الخارجي عبر Reverse Proxy أو المنفذ المباشر.
- **قراءة ملف `.env` التلقائية:** بدون الحاجة لتثبيت أي مكتبات خارجية، يتعرف التطبيق على أي متغيرات في ملف `.env`.
- **اتصال الويب سوكيت الديناميكي:** واجهة الويب تتصل بالسيرفر والويب سوكيت تلقائياً بناءً على النطاق والمنفذ الحالي (`location.host`).
- **حفظ واستقرار البيانات:** تخزين تلقائي للبيانات والرسائل في مجلد `data/` مع إمكانية الربط بـ Supabase للنسخ الاحتياطي إذا أردت.
